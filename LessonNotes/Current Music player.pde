//Dynamic Programming v Static
//
//Library - Minim
//
//Global Variables
int appWidth, appHeight;
float imageX, imageY, imageWidth, imageHeight;
//
void setup() {
    rect(imageX, imageY, imageWidth, imageHeight);
  //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.17;
  imageY = displayHeight*0.20;
  imageWidth = displayWidth*0.67;
  imageHeight = displayHeight*0.15;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.21;
  imageY = displayHeight*0.10;
  imageWidth = displayWidth*0.44;
  imageHeight = displayHeight*0.10;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.04;
  imageY = displayHeight*0.06;
  imageWidth = displayWidth*0.08;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight); //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.79;
  imageY = displayHeight*0.13;
  imageWidth = displayWidth*0.09;
  imageHeight = displayHeight*0.04;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.64;
  imageY = displayHeight*0.35;
  imageWidth = displayWidth*0.11;
  imageHeight = displayHeight*0.09;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.75;
  imageY = displayHeight*0.35;
  imageWidth = displayWidth*0.10;
  imageHeight = displayHeight*0.09;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.13;
  imageY = displayHeight*0.38;
  imageWidth = displayWidth*0.09;
  imageHeight = displayHeight*0.10;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.23;
  imageY = displayHeight*0.46;
  imageWidth = displayWidth*0.49;
  imageHeight = displayHeight*0.02;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
 //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.16;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.12;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.30;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.08;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
   //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.40;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.04;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.46;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.04;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.52;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.04;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.58;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.08;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.69;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.03;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.73;
  imageY = displayHeight*0.55;
  imageWidth = displayWidth*0.03;
  imageHeight = displayHeight*0.05;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
    //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.20;
  imageY = displayHeight*0.75;
  imageWidth = displayWidth*0.19;
  imageHeight = displayHeight*0.10;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.40;
  imageY = displayHeight*0.75;
  imageWidth = displayWidth*0.19;
  imageHeight = displayHeight*0.10;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
     //Display
  fullScreen();
  appWidth = displayWidth;
  appHeight = displayHeight;
  //
  imageX = displayWidth*0.60;
  imageY = displayHeight*0.75;
  imageWidth = displayWidth*0.20;
  imageHeight = displayHeight*0.10;
  //
  rect(imageX, imageY, imageWidth, imageHeight);
  //
} //End setup
//
void draw() {
  rect(imageX, imageY, imageWidth, imageHeight);
} //End draw
//
void mousePressed() {} //End mousePressed
//
void keyPressed() {} //End keyPressed
//
// End Main Program
